apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - service-green.yaml
  - servive-blue.yaml
  - rollout.yml

configMapGenerator:
  - name: catalog-config
    files:
      - application.yml
    options:
      labels:
        app: catalog-service
